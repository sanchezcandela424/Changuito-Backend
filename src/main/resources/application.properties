spring.application.name=buensabor

# Configuración de base de datos
spring.datasource.url=jdbc:postgresql://localhost:5432/buensabordb
spring.datasource.username=buensabor
spring.datasource.password=buensabor

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.default_schema=public


# Configuración de seguridad JWT
jwt.secret=JzG65GhH8nvBTrdbmQ3+LzMZkY3Re1y0N2nVwAGhJ3A=
jwt.expiration=3600

# Configuración de mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mi_email@gmail.com
spring.mail.password=mi_password_email

# Configuración de servicio externo (ej: MercadoPago)
mercadopago.accessToken=
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web.filter=DEBUG

# Google OAuth2 (Spring Security way)
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_SECRET_KEY}